# Log files
*.log
logs/

# IDE & Editor
.idea/
*.iml
*.iws
*.ipr
out/
.vscode/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100/
.Trashes/

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Docker / Env
docker-compose.override.yml
.env
# Everything under env/ stays local — the real env files hold credentials, and so do the
# .bak/.local/.orig variants and backup subdirectories that editors and merges leave behind.
# Only the *.env.example templates are tracked.
env/*
!env/*.env.example

# Temporary files
/tmp/
temp/
*.tmp

# Misc
*.swp
*~
